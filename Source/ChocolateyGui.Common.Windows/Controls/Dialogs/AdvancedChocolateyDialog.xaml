<MahDialogs:CustomDialog x:Class="ChocolateyGui.Common.Windows.Controls.Dialogs.AdvancedChocolateyDialog"
             xmlns:MahDialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <MahDialogs:CustomDialog.Content>
        <Grid Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0">Version</Label>
                <TextBox Grid.Row="0" Grid.Column="1" />
                <Label Grid.Row="1" Grid.Column="0">Package Parameters</Label>
                <TextBox Grid.Row="1" Grid.Column="1" />
                <Label Grid.Row="2" Grid.Column="0">Install Arguments</Label>
                <TextBox Grid.Row="2" Grid.Column="1" />
            </Grid>
            <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    x:Name="PART_AffirmativeButton"
                    Height="35"
                    MinWidth="80"
                    Margin="0,0,5,0"
                    mah:ControlsHelper.ContentCharacterCasing="Normal"
                    Click="PART_AffirmativeButton_OnClick"
                    Content="OK"
                    IsDefault="True"
                    Style="{DynamicResource MahApps.Styles.Button.Dialogs.Accent}" />
                <Button
                    x:Name="PART_NegativeButton"
                    Height="35"
                    MinWidth="80"
                    Margin="5,0,5,0"
                    mah:ControlsHelper.ContentCharacterCasing="Normal"
                    Click="PART_NegativeButton_OnClick"
                    Content="Cancel"
                    IsCancel="True"
                    Style="{DynamicResource MahApps.Styles.Button.Dialogs.Accent}" />
            </StackPanel>
        </Grid>
    </MahDialogs:CustomDialog.Content>
</MahDialogs:CustomDialog>
